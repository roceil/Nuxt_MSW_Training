{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "msw": "^1.2.1",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "@master/css": "^1.37.8",
    "dotenv": "^16.1.4"
  },
  "msw": {
    "workerDirectory": "public"
  }
}
